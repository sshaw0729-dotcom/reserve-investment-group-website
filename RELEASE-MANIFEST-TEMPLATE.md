# Release Manifest — Template

Duplicate this file per release as `RELEASE-MANIFEST-YYYY-MM-DD.md`.

- Release ID:
- Target branch: `main`
- Pull request(s) included:
- Production deployment ID (post-deploy):
- Deployed commit SHA:
- Pages added/changed:
- Compliance approvals attached (asset IDs + references):
- Required status checks: pass/fail summary
- Staging regression result:
- Rollback plan / prior stable deployment ID:
- Human approver(s) and date(s):
- Post-deploy verification: smoke test / forms / disclosures / PostHog
  events / canonical URLs / redirects / mobile / desktop / security headers
- Archive location:
