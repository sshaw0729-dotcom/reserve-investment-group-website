# One-off helper: generate package-lock.json via Actions (npm registry is reachable
# here but not from the local dev sandbox). Safe to delete once the lockfile is committed.
name: generate-lockfile
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --package-lock-only
      - run: git config user.name "github-actions[bot]" && git config user.email "github-actions[bot]@users.noreply.github.com" && git add package-lock.json && (git diff --cached --quiet || git commit -m "Generate package-lock.json") && git push
