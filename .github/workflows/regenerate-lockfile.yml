name: regenerate-next-lts-lockfile

on:
  push:
    branches: [security/next-lts-upgrade]

permissions:
  contents: write

jobs:
  regenerate:
    if: ${{ !contains(github.event.head_commit.message, '[lockfile-generated]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: security/next-lts-upgrade
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm install --package-lock-only --ignore-scripts --no-fund --no-audit
      - name: Commit regenerated lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Regenerate lockfile for Next.js 15.5.21 [lockfile-generated]"
          git push origin HEAD:security/next-lts-upgrade
